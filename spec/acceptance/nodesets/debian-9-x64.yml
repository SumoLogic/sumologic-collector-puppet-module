HOSTS:
  debian-9-x64:
    pe_dir:
    pe_ver:
    pe_upgrade_dir:
    pe_upgrade_ver:
    roles:
      - master
    platform: debian-9-amd64
    box: debian/stretch64
    box_url: https://app.vagrantup.com/debian/boxes/stretch64
    hypervisor: vagrant

CONFIG:
  log_level: verbose
